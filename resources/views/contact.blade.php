@include('partials.header')

<main>
    <div class="container">
        <h1>{{ __('Contact Us') }}</h1>
        <p>{{ __('Contact information or a contact form.') }}</p>
    </div>
</main>

@include('partials.footer')
